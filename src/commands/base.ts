let _: import("main/types").Command= {
    interaction: (i) => {return {flag: 'n'}},
    slash: undefined,
    description: "Base for file all commands",
    usage: "No Usage"
}

module.exports = _

export = _
